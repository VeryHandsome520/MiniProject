# ข้อเสนอโครงงาน: ระบบควบคุมและสั่งการ IoT หลายบอร์ดแบบรวมศูนย์
**(Universal Multi-Board IoT Control & Automation System)**

## 1. ชื่อโครงงาน
**"ระบบควบคุมและสั่งการ IoT หลายบอร์ดแบบรวมศูนย์"** (Universal Multi-Board IoT Control & Automation System)

## 2. วัตถุประสงค์ (Objectives)
1.  **การจัดการแบบรวมศูนย์ (Centralized Management)**: เพื่อสร้างหน้าเว็บแดชบอร์ดที่สามารถควบคุมและจัดการบอร์ด ESP32 หลายตัวได้พร้อมกันจากจุดเดียว ไม่ว่าอุปกรณ์จะอยู่ที่ไหน (ขอแค่มีอินเทอร์เน็ต)
2.  **การสั่งงานอัตโนมัติที่ยืดหยุ่น (Flexible Automation)**: เพื่อพัฒนาระบบควบคุมที่มากกว่าแค่เปิด-ปิด (On/Off) โดยเพิ่มโหมดตั้งเวลา (Timer) และโหมดกำหนดระยะเวลาทำงาน (Duration Timer)
3.  **การตรวจสอบสถานะแบบเรียลไทม์ (Real-Time Monitoring)**: เพื่อแสดงสถานะการเชื่อมต่อ (Online/Offline) ของแต่ละอุปกรณ์ และบันทึกประวัติการทำงาน (Log) เพื่อตรวจสอบย้อนหลัง
4.  **การปรับแต่ง User Experience (UX)**: เพื่อให้ผู้ใช้งานสามารถปรับเปลี่ยนธีมสี (Theme) และขนาดตัวอักษรของหน้าเว็บได้ตามความต้องการ (Accessibility)
5.  **การแจ้งเตือน (Notification)**: เพื่อเชื่อมต่อกับระบบ Telegram Bot สำหรับแจ้งเตือนทันทีเมื่อมีการทำงานตามเงื่อนไขอัตโนมัติ

## 3. ส่วนประกอบของชิ้นงาน (System Components)
### ฮาร์ดแวร์ (Hardware):
*   **ไมโครคอนโทรลเลอร์**: บอร์ด ESP32 (จำนวน 1 ตัวขึ้นไป)
*   **อุปกรณ์ทำงาน (Actuators)**: หลอด LED (ใช้จำลองการทำงานของเครื่องใช้ไฟฟ้า) ต่อกับขา GPIO ต่างๆ
*   **การเชื่อมต่อ**: WiFi Module (Built-in บน ESP32)
*   **แหล่งจ่ายไฟ**: USB Adapter 5V หรือ Power Bank

### ซอฟต์แวร์ (Software):
*   **เฟิร์มแวร์ (Board Firmware)**: เขียนด้วยภาษา C++ (Arduino Framework) โดยใช้ **FreeRTOS** เพื่อรองรับการทำงานแบบ Multitasking (แยกงานรับส่งข้อมูลกับงานควบคุมอุปกรณ์)
*   **ฝั่งเซิร์ฟเวอร์ (Backend)**: ภาษา PHP ทำงานร่วมกับฐานข้อมูล MySQL (MariaDB)
*   **หน้าเว็บ (Frontend)**: HTML5, CSS3 (Bootstrap 5), JavaScript (jQuery)
*   **API**: RESTful API (JSON format) สำหรับสื่อสารระหว่าง ESP32 และ Server

## 4. เงื่อนไขและขอบเขต (Conditions & Scope)

### 4.1 เงื่อนไขการทำงาน (Conditions)
1.  **การเชื่อมต่อ**: บอร์ด ESP32 ทุกตัวต้องเชื่อมต่อ WiFi ที่ออกอินเทอร์เน็ตได้ เพื่อส่งข้อมูลไปยัง Server กลาง
2.  **การระบุตัวตน**: ระบบจะระบุตัวอุปกรณ์โดยอัตโนมัติด้วย **MAC Address** (ไม่จำเป็นต้อง Hardcode ID ลงในโปรแกรม)
3.  **หลักการควบคุม**: Server ทำหน้าที่เป็น "สมองสั่งการ" โดย ESP32 จะทำการถาม (Poll) Server ทุกๆ 15 วินาที เพื่อรับคำสั่งล่าสุด
4.  **สถานะ Online**: หากบอร์ดขาดการติดต่อไปเกิน 15-20 วินาที หน้าเว็บจะแสดงสถานะเป็น Offline ทันที

### 4.2 ขอบเขตงาน (Scope)
*   **ควบคุมอุปกรณ์**: รองรับการเปิด-ปิด GPIO ได้หลายขาต่อ 1 บอร์ด (เช่น LED 2-3 ดวง)
*   **โหมดการทำงาน**:
    1.  **Manual**: สั่งเปิด-ปิดทันทีผ่านหน้าเว็บ
    2.  **Timer**: ตั้งเวลาเปิดและปิดตามช่วงเวลา (เช่น เปิด 18:00 - 06:00)
    3.  **Duration**: สั่งเปิดตามระยะเวลาที่กำหนด (เช่น เปิด 10 นาทีแล้วดับเอง)
*   **ปรับแต่งหน้าเว็บ**: ผู้ใช้สามารถเปลี่ยนสีพื้นหลัง, สีการ์ด, และขนาดตัวอักษรได้เอง โดยค่าจะถูกบันทึกไว้ใน Browser

---

## 5. คำอธิบาย Source Code (Source Code Explanation)

### 🟢 ส่วนที่ 1: เฟิร์มแวร์ ESP32 (`main.cpp`)
ทำหน้าที่เชื่อมต่อ WiFi และคอยรับคำสั่งจาก Server
*   `NetworkTask()`: ฟังก์ชันหลักที่ทำงานแยกอิสระ (FreeRTOS Task)
    *   ทำหน้าที่เชื่อมต่อ WiFi หากหลุดจะต่อใหม่เอง
    *   ใช้ `HTTPClient` ส่ง MAC Address และ IP ไปยัง Server (`POST /api_device.php`)
    *   รับค่า JSON ตอบกลับจาก Server และถอดรหัส (Parse JSON)
    *   สั่งงานขา GPIO (`digitalWrite`) ตามคำสั่งที่ได้รับ (ON/OFF)
    *   หน่วงเวลา 15 วินาทีต่อรอบ เพื่อไม่ให้ Server ทำงานหนักเกินไป

### 🟠 ส่วนที่ 2: ระบบจัดการอุปกรณ์ (`api_device.php`)
เป็น "หัวใจสำคัญ" ของฝั่ง Server ที่คุยกับบอร์ด
*   รับค่า MAC Address จากบอร์ด
*   ตรวจสอบในฐานข้อมูล:
    *   ถ้ายังไม่มี -> เพิ่มอุปกรณ์ใหม่ลงตาราง `devices`
    *   ถ้ามีแล้ว -> อัปเดตเวลาล่าสุด (`last_seen`) เพื่อบอกว่ายัง Online อยู่
*   **ประมวลผลโหมดอัตโนมัติ**:
    *   ตรวจสอบขา Pin ที่เป็นโหมด **TIMER**: เทียบเวลาปัจจุบันกับเวลาที่ตั้งไว้ ถ้าอยู่ในช่วง -> สั่งเปิด
    *   ตรวจสอบขา Pin ที่เป็นโหมด **DURATION**: ถ้ายังไม่หมดเวลา -> สั่งเปิด, ถ้าหมดแล้ว -> สั่งปิดและแจ้งเตือนผ่าน Telegram
*   ส่งคืนสถานะ (JSON Response) ให้บอร์ดนำไปทำตาม

### 🔵 ส่วนที่ 3: ระบบควบคุมและ UI
*   `index.php`: หน้า Dashboard รวม แสดงรายการอุปกรณ์ทั้งหมดและสถานะ Online/Offline
    *   ใช้ `common_ui.php` เพื่อโหลดการตั้งค่าธีม (Theme)
*   `board_view.php`: หน้าควบคุมอุปกรณ์รายตัว
    *   แสดงรายการ Pin ทั้งหมด
    *   ปุ่มสวิตช์ ON/OFF (Manual)
    *   ฟอร์มตั้งเวลา (Timer) และกำหนดระยะเวลา (Duration)
    *   ปุ่ม **+ Add Pin** และลบ Pin เพื่อจัดการขา GPIO ได้เอง
*   `api_control.php`: รับคำสั่งจากหน้าเว็บ (User Action) เช่น กดปุ่ม, ตั้งเวลา
    *   บันทึกค่าลงฐานข้อมูล
    *   ส่งข้อความแจ้งเตือนเข้า Telegram เมื่อมีการสั่งงานสำคัญ

---

## 6. คู่มือการใช้งาน (User Manual)

### ขั้นตอนที่ 1: การติดตั้งบอร์ด (Hardware Setup)
1.  จ่ายไฟเข้าบอร์ด ESP32 ผ่านสาย USB
2.  รอประมาณ 10-30 วินาที ให้บอร์ดเชื่อมต่อ WiFi (สังเกต LED บนบอร์ด หรือดูผ่าน Serial Monitor)

### ขั้นตอนที่ 2: การใช้งานหน้า Dashboard
1.  เปิด Browser เข้าไปที่ `http://smartbot-ai.com/3R/Earth/index.php`
2.  จะพบรายการบอร์ดที่เชื่อมต่ออยู่
    *   **สถานะสีเขียว (Online)**: บอร์ดกำลังทำงานปกติ
    *   **สถานะสีเทา (Offline)**: บอร์ดขาดการติดต่อ
3.  กดปุ่ม **Manage Board** เพื่อเข้าไปควบคุม

### ขั้นตอนที่ 3: การควบคุมอุปกรณ์ (Control Panel)
ในหน้าควบคุมรายบอร์ด สามารถทำได้ดังนี้:
*   **เปิด/ปิดทันที**: กดสวิตช์ Toggle เพิ่อสั่งงาน LED
*   **ตั้งเวลา (Schedule)**:
    1.  ใส่เวลาเริ่ม (ช่องซ้าย) และเวลาจบ (ช่องขวา)
    2.  กดปุ่ม **Set** -> ระบบจะเปลี่ยนเป็นโหมด Timer ทันที
*   **ตั้งเวลาถอยหลัง (Duration)**:
    1.  ใส่จำนวนนาทีในช่อง "Timer (Minutes)"
    2.  กดปุ่ม **Start** -> ไฟจะติดและดับเองเมื่อครบเวลา
*   **เพิ่ม/ลบ Pin**: กดปุ่ม "+ Add Pin" ด้านบนเพื่อเพิ่มขา GPIO ใหม่ที่ต้องการใช้งาน

### ขั้นตอนที่ 4: การปรับแต่งธีม (Theme Customization)
1.  กดปุ่มเฟือง **(⚙️)** ที่มุมขวาล่างของหน้าจอ
2.  เลือกปรับค่าได้ตามใจชอบ:
    *   Background Color: สีพื้นหลัง
    *   Card Color: สีของกล่องข้อความ
    *   Text Color: สีตัวอักษร
    *   Font Size: ขนาดตัวอักษร
3.  กด **Close** เพื่อใช้งาน (ค่าจะถูกบันทึกไว้อัตโนมัติ)

---
**จัดทำโดย**: Thanapat Lertpitaksit 67332310017-9
**วันที่**: 12 กุมภาพันธ์ 2026
